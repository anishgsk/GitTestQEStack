//To get status for an employee
String getStatus(String employeeID) {
	return getStatusForEmployee(employeeID);
}

//change in branch bug1
void getMessage() {
	//sdsfads
}

//change by team mate on remote master to test push with conflict scenario

//change in master branch
void getCTC() {
	return CTC;
}
